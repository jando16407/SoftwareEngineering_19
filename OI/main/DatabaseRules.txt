service cloud.firestore {
  match /databases/{database}/documents {
    //match /{document=**} {
      //allow read, write;
    //}
    // match docs in the item collection
    match /item/{itemID}{
      allow read, write: if request.auth.uid != null;
    }
  }
}